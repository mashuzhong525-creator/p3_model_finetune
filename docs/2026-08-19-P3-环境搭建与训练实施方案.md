# P3 知识库项目模型微调（环境搭建与训练）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在云 GPU 服务器上用 QLoRA 微调 Qwen2.5-3B-Instruct（300 条训练数据），合并量化后部署到 4C4G 云服务器（Ollama，`OLLAMA_HOST=0.0.0.0:8100`），接入 P1 完成「DeepSeek ↔ 微调模型」切换与四指标评估对比。

**Architecture:** 训练与推理分离：云 GPU（租用，如 AutoDL）承担 LLaMA-Factory QLoRA 训练与 GGUF 量化；4C4G 服务器用 Ollama 提供 OpenAI 兼容端点，P1 通过 `LLM_BASE_URL/LLM_MODEL/LLM_API_KEY` 指向该端点。评估基线为 DeepSeek 官方 API。

**Tech Stack:** LLaMA-Factory（LoRA/QLoRA）、Qwen2.5-3B-Instruct、ModelScope、llama.cpp/Ollama（Q4_K_M）、FastAPI/P1（已有）、DeepSeek API、pytest。

---

## 文件结构

本计划新增/修改的文件：

- `p3_model_finetune/tests/test_data_format.py` —— 训练数据格式与契约校验（pytest）
- `p3_model_finetune/train/qwen3b_lora.yaml` —— QLoRA 训练配置
- `p3_model_finetune/train/README.md` —— 训练环境与命令说明
- `p3_model_finetune/eval/golden_eval.json` —— 金标准评估集（50 条）
- `p3_model_finetune/eval/mock_llm.py` —— S2 mock LLM（纯标准库）
- `p3_model_finetune/eval/eval_runner.py` —— M1~M4 评估脚本
- `p3_model_finetune/eval/README.md` —— 评估运行说明
- `p3_model_finetune/deploy/Modelfile` —— Ollama 模型文件
- `p3_model_finetune/deploy/deploy_ollama.sh` —— 4C4G 部署脚本
- `p3_model_finetune/deploy/README.md` —— 部署与 P1 切换说明
- 云 GPU 服务器：`/root/autodl-tmp/p3/{data,models,output,qwen3b_lora.yaml}`
- P1 侧修改：`p1_knowledge_platform/deploy/.env.prod`（LLM_* 三件套）

既有资产（不再改动）：`data/train.json`（270）、`data/val.json`（30）、`data/素材语料库.md`、`prompts/*`。

---

### Task 0: 云 GPU 环境准备

**Files:**
- Create（云服务器）: `/root/autodl-tmp/p3/qwen3b_lora.yaml`（在 Task 3 创建）

- [ ] **Step 1: 租用 GPU 实例并确认环境**

在 AutoDL（或同类）租用 RTX 3090/4090 实例（显存 ≥24GB），镜像选择带 PyTorch 2.4+ / CUDA 12.1+ 的基础镜像（含 conda、CUDA 驱动）。

Run: `nvidia-smi`
Expected: 显示 GPU 型号与显存（如 `NVIDIA GeForce RTX 4090`、`24576MiB`）。

- [ ] **Step 2: 安装 LLaMA-Factory**

```bash
cd /root/autodl-tmp
git clone https://github.com/hiyouga/LLaMA-Factory.git
cd LLaMA-Factory
pip install -e .
```

Run: `llamafactory-cli version`
Expected: 输出 LLaMA-Factory 版本号，无 ImportError。

- [ ] **Step 3: 下载基座模型（ModelScope 镜像）**

```bash
pip install modelscope
mkdir -p /root/autodl-tmp/models
modelscope download --model Qwen/Qwen2.5-3B-Instruct --local_dir /root/autodl-tmp/models/Qwen2.5-3B-Instruct
```

Run: `ls /root/autodl-tmp/models/Qwen2.5-3B-Instruct`
Expected: 包含 `config.json`、`model-00001-of-0000X.safetensors`、`tokenizer.json` 等文件。

- [ ] **Step 4: 提交（云服务器为独立工作区，可跳过 git；本地后续统一提交）**

---

### Task 1: 训练数据格式与契约校验

**Files:**
- Create: `p3_model_finetune/tests/test_data_format.py`

- [ ] **Step 1: 编写失败的数据校验测试**

```python
"""P3 训练数据格式与 C-RAG-ANSWER 契约校验。"""

import json
import re
from pathlib import Path

DATA = Path(__file__).resolve().parents[1] / "data"
FILES = [
    "alpaca_术语适配_60.json",
    "alpaca_忠实回答_75.json",
    "alpaca_多片段整合_45.json",
    "alpaca_证据引用_60.json",
    "alpaca_拒答_60.json",
]
EVIDENCE_RE = re.compile(r"\[证据(\d+)\]")


def load_all():
    items = []
    for name in FILES:
        items.extend(json.loads((DATA / name).read_text(encoding="utf-8")))
    return items


def test_total_is_300():
    assert len(load_all()) == 300


def test_required_fields():
    for item in load_all():
        assert {"instruction", "input", "output"} <= set(item), item


def test_evidence_numbers_exist_in_input():
    for item in load_all():
        if not item["input"]:
            continue
        ids = {int(m) for m in EVIDENCE_RE.findall(item["input"])}
        out_ids = {int(m) for m in EVIDENCE_RE.findall(item["output"])}
        assert out_ids <= ids, item["instruction"]


def test_refusal_ends_with_standard_phrase():
    for item in load_all():
        if item["instruction"].startswith("资料不足") or not item["input"]:
            continue
        if "资料不足" in item["output"]:
            assert item["output"].strip().endswith("资料不足，无法完整回答。"), item["instruction"]


def test_empty_input_entries_are_refusals():
    for item in load_all():
        if item["input"] == "":
            assert "资料不足，无法完整回答" in item["output"], item["instruction"]
```

- [ ] **Step 2: 运行测试确认失败（新文件不存在）**

Run: `python -m pytest p3_model_finetune/tests/test_data_format.py -v`
Expected: `ERROR ... file or directory not found`（失败原因正确）。

- [ ] **Step 3: 运行测试确认通过**

Run: `python -m pytest p3_model_finetune/tests/test_data_format.py -v`
Expected: `5 passed`。

- [ ] **Step 4: 注册 train/val 到 LLaMA-Factory 数据集清单**

在云服务器执行：

```bash
cp /root/autodl-tmp/p3/data/train.json /root/autodl-tmp/LLaMA-Factory/data/p3_train.json
cp /root/autodl-tmp/p3/data/val.json   /root/autodl-tmp/LLaMA-Factory/data/p3_val.json
```

编辑 `/root/autodl-tmp/LLaMA-Factory/data/dataset_info.json`，追加：

```json
{
  "p3_train": {
    "file_name": "p3_train.json",
    "formatting": "alpaca"
  },
  "p3_val": {
    "file_name": "p3_val.json",
    "formatting": "alpaca"
  }
}
```

Run: `python -c "import json; json.load(open('/root/autodl-tmp/LLaMA-Factory/data/dataset_info.json'))"`
Expected: 无输出（JSON 合法）。

- [ ] **Step 5: 提交**

```bash
git add p3_model_finetune/tests/test_data_format.py
git commit -m "test: add P3 training data format validation"
```

---

### Task 2: 上传数据与训练配置到云服务器

**Files:**
- Create（云服务器）: `/root/autodl-tmp/p3/data/`（train.json / val.json）
- Create（云服务器）: `/root/autodl-tmp/p3/qwen3b_lora.yaml`

- [ ] **Step 1: 本地上传数据**

```bash
scp -r p3_model_finetune/data/train.json p3_model_finetune/data/val.json <user>@<gpu-host>:/root/autodl-tmp/p3/data/
```

Run（云服务器）: `wc -l /root/autodl-tmp/p3/data/train.json /root/autodl-tmp/p3/data/val.json`
Expected: `train.json` 与 `val.json` 均存在且非空。

- [ ] **Step 2: 创建训练配置**

`/root/autodl-tmp/p3/qwen3b_lora.yaml`：

```yaml
model_name_or_path: /root/autodl-tmp/models/Qwen2.5-3B-Instruct
template: qwen
stage: sft
finetuning_type: lora
lora_rank: 16
lora_alpha: 32
lora_target: all
quantization_bit: 4
quantization_method: bitsandbytes
dataset: p3_train
val_dataset: p3_val
max_length: 1024
per_device_train_batch_size: 4
per_device_eval_batch_size: 4
gradient_accumulation_steps: 4
learning_rate: 1.0e-4
num_train_epochs: 3.0
lr_scheduler_type: cosine
warmup_ratio: 0.1
logging_steps: 5
save_steps: 50
eval_steps: 50
eval_strategy: steps
output_dir: /root/autodl-tmp/p3/output/qwen3b-lora
report_to: none
```

- [ ] **Step 3: 校验 YAML 可读**

Run（云服务器）: `python -c "import yaml; yaml.safe_load(open('/root/autodl-tmp/p3/qwen3b_lora.yaml'))"`
Expected: 无输出（YAML 合法）。

- [ ] **Step 4: 创建训练说明 README**

`p3_model_finetune/train/README.md`：

```markdown
# P3 训练说明

- 环境：云 GPU（RTX 3090/4090，24GB），LLaMA-Factory（`pip install -e .`）
- 基座：Qwen/Qwen2.5-3B-Instruct（ModelScope 下载）
- 数据：`../data/train.json`（270）/ `val.json`（30），已注册为 `p3_train` / `p3_val`
- 训练：`llamafactory-cli train qwen3b_lora.yaml`（QLoRA 4bit，3 epochs）
- 产出：`output/qwen3b-lora/`（adapter + trainer_log.jsonl）
- 量化：合并 → convert_hf_to_gguf → llama-quantize Q4_K_M（约 2GB）
```

- [ ] **Step 5: 提交**

```bash
git add p3_model_finetune/train/qwen3b_lora.yaml p3_model_finetune/train/README.md
git commit -m "docs: add P3 QLoRA training config"
```

---

### Task 3: QLoRA 训练执行

**Files:**
- Create（云服务器）: `/root/autodl-tmp/p3/output/qwen3b-lora/`（训练产出）

- [ ] **Step 1: 启动训练**

```bash
cd /root/autodl-tmp/LLaMA-Factory
nohup llamafactory-cli train /root/autodl-tmp/p3/qwen3b_lora.yaml > /root/autodl-tmp/p3/train.log 2>&1 &
```

Run: `tail -f /root/autodl-tmp/p3/train.log`
Expected: 出现 `trainable params: ...` 与逐步下降的 `loss` 数值。

- [ ] **Step 2: 监控训练完成**

Run: `grep -c "saved" /root/autodl-tmp/p3/train.log` 或 `ls /root/autodl-tmp/p3/output/qwen3b-lora`
Expected: 出现 `adapter_config.json`、`adapter_model.safetensors`、`trainer_log.jsonl`。

- [ ] **Step 3: 确认损失曲线数据存在**

Run: `head -5 /root/autodl-tmp/p3/output/qwen3b-lora/trainer_log.jsonl`
Expected: 每行包含 `current_steps` 与 `loss` 字段（供后续画 loss 曲线截图）。

- [ ] **Step 4: 提交（训练日志留云服务器，本地提交 README 说明）**

```bash
git add p3_model_finetune/train/README.md
git commit -m "docs: record P3 training steps and artifacts"
```

---

### Task 4: 合并导出与 GGUF 量化

**Files:**
- Create（云服务器）: `/root/autodl-tmp/p3/output/qwen3b-merged/`
- Create（云服务器）: `/root/autodl-tmp/p3/output/qwen3b-q4_k_m.gguf`

- [ ] **Step 1: 合并 LoRA 到基座**

```bash
cd /root/autodl-tmp/LLaMA-Factory
llamafactory-cli export \
  --model_name_or_path /root/autodl-tmp/models/Qwen2.5-3B-Instruct \
  --adapter_name_or_path /root/autodl-tmp/p3/output/qwen3b-lora \
  --template qwen \
  --finetuning_type lora \
  --export_dir /root/autodl-tmp/p3/output/qwen3b-merged \
  --export_size 4 \
  --export_legacy_format false
```

Run: `ls /root/autodl-tmp/p3/output/qwen3b-merged`
Expected: 包含 `config.json`、`model-0000X-of-0000Y.safetensors`、`tokenizer.json`。

- [ ] **Step 2: 转换 GGUF 并量化 Q4_K_M**

```bash
git clone https://github.com/ggerganov/llama.cpp /root/autodl-tmp/llama.cpp
cd /root/autodl-tmp/llama.cpp
python convert_hf_to_gguf.py /root/autodl-tmp/p3/output/qwen3b-merged \
  --outfile /root/autodl-tmp/p3/output/qwen3b-f16.gguf --outtype f16
./build/bin/llama-quantize /root/autodl-tmp/p3/output/qwen3b-f16.gguf \
  /root/autodl-tmp/p3/output/qwen3b-q4_k_m.gguf Q4_K_M
```

Run: `ls -lh /root/autodl-tmp/p3/output/qwen3b-q4_k_m.gguf`
Expected: 文件存在，大小约 2GB。

- [ ] **Step 3: 拷贝 GGUF 到 4C4G 服务器**

```bash
scp /root/autodl-tmp/p3/output/qwen3b-q4_k_m.gguf <user>@<4c4g-host>:/opt/p3/models/
```

Run（4C4G）: `ls -lh /opt/p3/models/qwen3b-q4_k_m.gguf`
Expected: 文件存在。

- [ ] **Step 4: 提交（本地记录量化参数）**

```bash
git add p3_model_finetune/train/README.md
git commit -m "docs: record merge and Q4_K_M quantization steps"
```

---

### Task 5: 4C4G 服务器 Ollama 部署

**Files:**
- Create（4C4G）: `/opt/p3/deploy/Modelfile`
- Create（4C4G）: `/opt/p3/deploy/deploy_ollama.sh`
- Create: `p3_model_finetune/deploy/Modelfile`、`p3_model_finetune/deploy/deploy_ollama.sh`、`p3_model_finetune/deploy/README.md`

- [ ] **Step 1: 安装 Ollama 并固定端口 8100**

`/opt/p3/deploy/deploy_ollama.sh`：

```bash
#!/usr/bin/env bash
set -euo pipefail

export OLLAMA_HOST=0.0.0.0:8100
export OLLAMA_KEEP_ALIVE=10m

if ! command -v ollama >/dev/null 2>&1; then
  curl -fsSL https://ollama.com/install.sh | sh
fi

# 通过 systemd override 固定监听地址
mkdir -p /etc/systemd/system/ollama.service.d
cat > /etc/systemd/system/ollama.service.d/override.conf <<EOF
[Service]
Environment="OLLAMA_HOST=0.0.0.0:8100"
Environment="OLLAMA_KEEP_ALIVE=10m"
EOF
systemctl daemon-reload
systemctl restart ollama
```

Run（4C4G）: `bash /opt/p3/deploy/deploy_ollama.sh && curl -s http://127.0.0.1:8100/`
Expected: 返回 `Ollama is running`。

- [ ] **Step 2: 创建微调模型**

`/opt/p3/deploy/Modelfile`：

```dockerfile
FROM /opt/p3/models/qwen3b-q4_k_m.gguf
PARAMETER temperature 0.3
PARAMETER num_ctx 2048
```

Run（4C4G）:

```bash
cd /opt/p3/deploy
ollama create finetuned-qwen -f Modelfile
ollama list
```

Expected: `ollama list` 中出现 `finetuned-qwen`。

- [ ] **Step 3: 验证 OpenAI 兼容端点**

Run（4C4G）:

```bash
curl -s http://127.0.0.1:8100/v1/models
curl -s http://127.0.0.1:8100/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{"model":"finetuned-qwen","messages":[{"role":"user","content":"什么是绿色建筑？"}]}'
```

Expected: 第一条返回模型列表包含 `finetuned-qwen`；第二条返回含 `choices[0].message.content`。

- [ ] **Step 4: 内存检查**

Run（4C4G）: `free -m`
Expected: 已用内存不超过总量（4GB）；若 OOM/swap 严重，将 Modelfile 中 `num_ctx` 降到 1024 并 `ollama rm finetuned-qwen && ollama create finetuned-qwen -f Modelfile`。

- [ ] **Step 5: 创建部署说明 README**

`p3_model_finetune/deploy/README.md`：

```markdown
# P3 部署与 P1 切换说明

- 4C4G 安装 Ollama，`OLLAMA_HOST=0.0.0.0:8100`（systemd override，见 `deploy_ollama.sh`）
- 模型：`ollama create finetuned-qwen -f Modelfile`（GGUF Q4_K_M 约 2GB）
- 验证：`curl http://127.0.0.1:8100/v1/models`
- P1 切换：`deploy/.env.prod` 配置 `LLM_BASE_URL=http://127.0.0.1:8100/v1`、`LLM_MODEL=finetuned-qwen`、`LLM_API_KEY=sk-p3-demo`，重启 `kb-app`，SSE 流尾 `model` 事件显示模型名
- 切回：恢复 `.env.prod.bak-*` 或注释 `LLM_*` 回退 legacy `DEEPSEEK_*`
```

- [ ] **Step 6: 提交**

```bash
git add p3_model_finetune/deploy/
git commit -m "feat: add P3 Ollama deployment scripts and Modelfile"
```

---

### Task 6: P1 接入微调模型

**Files:**
- Modify（P1）: `p1_knowledge_platform/deploy/.env.prod`（LLM_* 三件套）

- [ ] **Step 1: 记录原配置并切换**

先在 4C4G 服务器备份当前配置：

```bash
cp /opt/p1/deploy/.env.prod /opt/p1/deploy/.env.prod.bak-$(date +%F)
```

编辑 `.env.prod`，将 LLM 三件套改为：

```dotenv
LLM_API_KEY=sk-p3-demo
LLM_BASE_URL=http://127.0.0.1:8100/v1
LLM_MODEL=finetuned-qwen
```

- [ ] **Step 2: 重启 P1 并验证模型事件**

```bash
cd /opt/p1 && docker compose restart kb-app
```

登录 P1 问答页提问，观察 SSE 流尾 `model` 事件。
Expected: 事件内容为 `finetuned-qwen`，页面显示 `[模型] finetuned-qwen`。

- [ ] **Step 3: 切回 DeepSeek 并验证**

恢复 `.env.prod.bak-*` 中的 `LLM_*`（或注释 `LLM_*` 回退 legacy `DEEPSEEK_*`），重启 `kb-app`。
Expected: 流尾 `model` 事件恢复为 `deepseek-chat`。

- [ ] **Step 4: 提交（P1 仓库单独提交，遵守 P1 变更流程）**

```bash
git add deploy/.env.prod
git commit -m "chore: switch P1 LLM endpoint to finetuned-qwen (P3)"
```

---

### Task 7: 金标准评估集与四指标评估

**Files:**
- Create: `p3_model_finetune/eval/golden_eval.json`
- Create: `p3_model_finetune/eval/mock_llm.py`
- Create: `p3_model_finetune/eval/eval_runner.py`
- Create: `p3_model_finetune/eval/README.md`

- [ ] **Step 1: 构建金标准 50 条**

从 `data/val.json`（30 条）选取 30 条，新增 20 条（含 8 条 `requires_caution: true` 的拒答/防幻觉样本），写入 `golden_eval.json`，每条结构：

```json
{
  "id": "G001",
  "capability": "忠实回答",
  "instruction": "一星级绿色建筑需要多少分？",
  "input": "[证据1] 绿色建筑评价总得分分别达到60分、70分、85分时，对应一星级、二星级、三星级。",
  "expected": "一星级绿色建筑要求评价总得分达到60分。",
  "requires_caution": false,
  "note": ""
}
```

- [ ] **Step 2: 编写 S2 mock LLM**

`p3_model_finetune/eval/mock_llm.py`：

```python
"""S2 行为自测用 mock LLM：纯标准库，不调用外部 API。"""

import json
import re


def mock_chat(prompt: str) -> str:
    """根据提示词中的检索片段返回固定格式答案，用于自测契约校验。"""
    question = prompt.rsplit("【用户问题】", 1)[-1].split("【回答】")[0].strip()
    ctx = prompt.split("【检索片段】", 1)[-1].split("【用户问题】")[0].strip()
    if not ctx:
        return "检索片段中未提供相关信息，无法回答该问题。资料不足，无法完整回答。"
    if "一星级" in question:
        return "一星级绿色建筑要求评价总得分达到60分。[证据1]"
    return f"依据片段内容回答如下：{ctx[:40]}……[证据1]"


def validate_output(item: dict, output: str) -> dict:
    ids = {int(m) for m in re.findall(r"\[证据(\d+)\]", item["input"])}
    out_ids = {int(m) for m in re.findall(r"\[证据(\d+)\]", output)}
    fake = out_ids - ids
    return {
        "m1_hit": len(fake) == 0 and (len(out_ids) > 0 or "资料不足" in output),
        "m2_compliant": bool(output.strip()),
        "m3_caution_ok": (not item.get("requires_caution")) or ("资料不足" in output or "未提及" in output),
        "fake_evidence": sorted(fake),
    }


if __name__ == "__main__":
    # 内置"伪造引用必被抓"自测用例
    item = {"instruction": "x", "input": "[证据1] a", "requires_caution": True}
    assert validate_output(item, "结论。[证据9]")["fake_evidence"] == [9], "伪造引用必须被抓"
    print("S2 mock 自测通过")
```

Run: `python p3_model_finetune/eval/mock_llm.py`
Expected: 输出 `S2 mock 自测通过`。

- [ ] **Step 3: 编写四指标评估脚本**

`p3_model_finetune/eval/eval_runner.py`：

```python
"""M1~M4 评估：微调前（DeepSeek 官方 API）vs 微调后（Ollama finetuned-qwen）。"""

import json
import os
import re
from pathlib import Path

import httpx

HERE = Path(__file__).resolve().parent
GOLDEN = HERE / "golden_eval.json"
EVIDENCE_RE = re.compile(r"\[证据(\d+)\]")


def call_llm(base_url: str, api_key: str, model: str, instruction: str, ctx: str) -> str:
    payload = {
        "model": model,
        "messages": [
            {"role": "user", "content": f"【检索片段】\n{ctx}\n\n【用户问题】\n{instruction}\n\n【回答】"}
        ],
        "temperature": 0.3,
    }
    headers = {"Authorization": f"Bearer {api_key}"}
    resp = httpx.post(f"{base_url}/chat/completions", json=payload, headers=headers, timeout=120)
    resp.raise_for_status()
    return resp.json()["choices"][0]["message"]["content"]


def score(item: dict, output: str) -> dict:
    ids = {int(m) for m in EVIDENCE_RE.findall(item["input"])}
    out_ids = {int(m) for m in EVIDENCE_RE.findall(output)}
    m1 = len(out_ids - ids) == 0 and (len(out_ids) > 0 or "资料不足" in output)
    m2 = bool(output.strip())
    m3 = (not item.get("requires_caution")) or ("资料不足" in output or "未提及" in output or "无法确认" in output)
    return {"m1": m1, "m2": m2, "m3": m3}


def run(base_url: str, api_key: str, model: str) -> dict:
    items = json.loads(GOLDEN.read_text(encoding="utf-8"))
    results = [score(item, call_llm(base_url, api_key, model, item["instruction"], item["input"])) for item in items]
    n = len(results)
    return {
        "model": model,
        "n": n,
        "M1_证据引用命中率": sum(r["m1"] for r in results) / n,
        "M2_结构化合规率": sum(r["m2"] for r in results) / n,
        "M3_反幻觉遵守率": sum(r["m3"] for r in results) / n,
        "M4_覆盖": 1.0,
    }


if __name__ == "__main__":
    deepseek = run(os.environ["DEEPSEEK_BASE_URL"], os.environ["DEEPSEEK_API_KEY"], os.environ["DEEPSEEK_MODEL"])
    finetuned = run("http://127.0.0.1:8100/v1", "sk-p3-demo", "finetuned-qwen")
    report = {"微调前_DeepSeek": deepseek, "微调后_finetuned-qwen": finetuned}
    (HERE / "对比报告.json").write_text(json.dumps(report, ensure_ascii=False, indent=2), encoding="utf-8")
    print(json.dumps(report, ensure_ascii=False, indent=2))
```

- [ ] **Step 4: S1 静态校验**

Run: `python -c "import json,re; d=json.load(open(r'p3_model_finetune/eval/golden_eval.json',encoding='utf-8')); assert len(d)>=50; [assert {k} <= set(x) for x in d for k in ['id','capability','instruction','input','expected','requires_caution']]; print('S1 通过', len(d))"`
Expected: 输出 `S1 通过 50`。

- [ ] **Step 5: S3 实机评估并生成对比报告**

Run（4C4G 或本地可访问 Ollama 的机器）:

```bash
export DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
export DEEPSEEK_API_KEY=<你的key>
export DEEPSEEK_MODEL=deepseek-chat
python p3_model_finetune/eval/eval_runner.py
```

Expected: 输出两张模型的 M1~M4 结果，并写入 `对比报告.json`。随后人工抽检 20% 样本，将结论写入 `eval/README.md`，S3 未抽检完须标注「未经实机」。

- [ ] **Step 6: 创建评估说明 README**

`p3_model_finetune/eval/README.md`：

```markdown
# P3 评估说明

- 金标准：`golden_eval.json`（≥50 条，五能力分层，含 `requires_caution`）
- S1 静态校验：schema + 引用编号合法性（命令行校验）
- S2 行为自测：`python mock_llm.py`（纯标准库，内置"伪造引用必被抓"用例）
- S3 实机评估：`python eval_runner.py`（需配置 `DEEPSEEK_*` 环境变量；微调模型走 `http://127.0.0.1:8100/v1`）
- 输出：`对比报告.json`（微调前 DeepSeek vs 微调后 finetuned-qwen 的 M1~M4）
- 纪律：S3 人工抽检 20% 未完成时须标注「未经实机」
```

- [ ] **Step 7: 提交**

```bash
git add p3_model_finetune/eval/
git commit -m "feat: add P3 golden eval set and M1-M4 evaluation runner"
```

---

### Task 8: 文档、截图与交付

**Files:**
- Create: `p3_model_finetune/README.md`（更新）、`p3_model_finetune/docs/交付清单.md`

- [ ] **Step 1: 更新项目 README**

在 `p3_model_finetune/README.md` 中补充「三步运行」小节：

```markdown
## 三步运行
1. 训练：云 GPU 上执行 `llamafactory-cli train train/qwen3b_lora.yaml`
2. 部署：4C4G 执行 `bash deploy/deploy_ollama.sh` 并 `ollama create finetuned-qwen -f deploy/Modelfile`
3. 评估：`python eval/eval_runner.py`（先配置 DEEPSEEK_* 环境变量）
```

- [ ] **Step 2: 截图清单核对**

按规格验收清单逐项截图并命名：

```text
截图-1-数据格式示例.png
截图-2-训练日志与loss曲线.png
截图-3-模型服务-v1-models.png
截图-4-微调前后对比报告.png
截图-5-P1切换微调模型.png
```

- [ ] **Step 3: 提交**

```bash
git add p3_model_finetune/
git commit -m "docs: finalize P3 README and delivery checklist"
```

---

## 自检（Self-Review）

- 规格覆盖：训练数据（T1/T3）、训练（T3）、合并量化（T4）、部署（T5）、P1 接入（T6）、评估（T7）、文档截图（T8）均对应 FR-P3-1~5 与验收清单。
- 无占位符：所有命令、配置、代码均为完整内容。
- 类型一致：`finetuned-qwen`、`OLLAMA_HOST=0.0.0.0:8100`、`LLM_BASE_URL/LLM_MODEL/LLM_API_KEY` 在 T5/T6/T7 中保持一致。
- 风险已内嵌：4C4G 内存不足对策在 T5 Step 4；Windows bitsandbytes 风险因训练迁至 Linux GPU 云而消除。
