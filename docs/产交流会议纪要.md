# 产交流会议纪要（P3 知识库项目模型微调）

> 会议时间：2026-08-19 ｜ 参与方：用户（项目负责人）＋ Codex（需求梳理 / 技术评估）
> 状态：进行中；本文档保存决策与开放问题，提示词另存于 `prompts/`

## 1. 会议背景

按《项目实战》4.1 知识库项目模型微调要求，结合已上线的 P1 知识库管理平台，确定 P3 的技术栈与实施方案。已知硬约束：云服务器 4核4G 且已运行 P1；本地为 RTX 5060 Laptop 8GB 显存。

## 2. 已确认决策（D）

| 编号 | 决策 | 结论 | 状态 |
| --- | --- | --- | --- |
| D1 | 技术栈主干 | 方案A：本地训练 + 微调模型部署回 4C4G 服务器做 CPU 量化推理 | ✅ 已确认 |
| D2 | 训练环境 | 本地 RTX 5060 Laptop 8GB；环境体检通过（CUDA 冒烟测试 20 步 0.49s） | ✅ 已确认 |
| D3 | 推理部署 | 4C4G 云服务器，Ollama/llama.cpp 提供 OpenAI 兼容接口，Q4 量化 | ✅ 方向确认（端口细节见 O4） |
| D4 | 训练算力 | 三次修订（2026-08-19）：租用云 GPU 一天（按量）训练 1.5B | ✅ 已确认（三次修订） |
| D5 | 基座型号 | 三次修订（2026-08-19）：Qwen2.5-1.5B-Instruct（基座不变） | ✅ 已确认（三次修订） |
| D6 | 评估方案 | 金标准 ≥50 条 + M1~M4 + S1/S2/S3，微调前后对比 | ✅ 依据任务框架确认 |
| D7 | 评估基线 | 微调前对比对象 = DeepSeek 官方 API（2026-08-19 确认） | ✅ 已确认 |
| D8 | 训练语料 | 直接用 Codex 整理的公开语料生成 300 条问答对（O2 选项 2/3，2026-08-19 确认） | ✅ 已确认 |
| D9 | 标准口径 | 现行标准 = GB/T 50378-2019（2024年版），2024-10-01 起实施 | ✅ 已确认 |
| D10 | 训练数据 | 已生成 300 条问答对（术语60/忠实75/整合45/引用60/拒答60），train/val=270/30，存于 `data/`；构造冲突样本已替换为无召回/弱相关 | ✅ 已完成 |
| D11 | 推理部署 | 本地 Windows Ollama（`OLLAMA_HOST=127.0.0.1:8100`），P1 线上不切换（2026-08-19 三次修订） | ✅ 已确认 |

## 3. 开放问题（O，待用户确认）

| 编号 | 问题 | 影响 | 备注 |
| --- | --- | --- | --- |
| O4 | Ollama 端口 11434 如何对齐契约 8100？ | 决定 P1 配置与部署细节 | 2026-08-19 三次修订：本地 Ollama，OLLAMA_HOST=127.0.0.1:8100 |
| O5 | 是否额外训练 3B 做效果对比？ | 增加工时 | N/A：主选模型已为 1.5B，不再需要对比训练 |

> O1 已确认（2026-08-19 二次修订）：基座 = Qwen2.5-1.5B-Instruct；本地训练 LoRA bf16。
> O2 已确认（2026-08-19）：训练语料 = 使用 Codex 整理的公开语料（`p3_model_finetune/data/素材语料库.md`）直接生成 300 条问答对。
> O3 已确认（2026-08-19）：评估基线 = DeepSeek 官方 API。
> 修订（2026-08-19）：基座改为 Qwen2.5-3B-Instruct，训练改为云 GPU 服务器；O4 确认 OLLAMA_HOST=0.0.0.0:8100。原 O1=1.5B 决策作废。
> 二次修订（2026-08-19）：训练改回本地（Windows 本机 RTX 5060 8GB），基座改为 Qwen2.5-1.5B-Instruct，方式为 LoRA bf16；租 GPU 云路径废弃。部署不变（4C4G Ollama 8100）。
> 三次修订（2026-08-19）：训练改为租用云 GPU 一天（按量，如 AutoDL），推理部署改为本地 Windows Ollama（127.0.0.1:8100），P1 线上不切换，仅本地演示与评估对比。

## 4. 环境体检结论摘要

- 硬件：i7-13700HX（16C/24T）、16GB 内存、RTX 5060 Laptop 8GB（sm_120）、驱动 610.74 / CUDA 13.3、D: 剩余 336GB。
- 软件：base 为 torch CPU 版；`mypytorch`（torch 2.11+cu130）与 `nlp`（torch 2.11+cu128）GPU 可用；均缺 bitsandbytes/trl/LLaMA-Factory；已下载 bge-m3 与 bge-reranker-large（10.7GB）。
- 结论：硬件可训练 1.5B/3B（3B 需 QLoRA）；软件需补装；详见《本地环境体检报告.md》。

## 5. 过程说明

- 本纪要随规格同步更新；任何新增决策均追加到第 2/3 节。
- 训练数据生成与评估所用提示词统一存放 `prompts/`，保证可追溯。
